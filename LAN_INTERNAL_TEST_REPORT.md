# 内网横向攻击压测报告（非 root 场景）

> 测试日期：内网 + 公网组合压测
> 场景：模拟攻击者已获取内网同网段服务器（**普通用户权限，非 root**），
> 对内网目标发起组合攻击；同时公网侧 5 台节点协同（root，与目标同城同运营商）。

---

## 1. 测试目标与配置

| 角色 | 规格 | 权限 | 网络位置 |
|---|---|---|---|
| 目标靶机 | 8 核 16G | 常规业务配置 | 内网，同时保留公网入口 |
| 内网攻击节点 | 10Gbps 网卡 | **非 root**（模拟被控低权限主机） | 与目标同网段，RTT < 2ms |
| 公网攻击节点 | 5 × 500Mbps 出口 | root | 与目标同城同电信，RTT ≈ 3-8ms |

**攻击组合**（双任务并行，同一目标）：

- 内网任务（仅内网节点）：`udp_stdhex`(300 线程) + `http_flood`(100) + `http2_reset`(100) + `tcp_connect`(100) + `tcp_ack`(100)
- 公网任务（仅公网 5 节点）：`udp_stdhex`(100) + `http2_reset`(50) + `http_flood`(30) + `udp_burst`(20)

**约束**：内网节点非 root —— 无 raw socket（SYN 洪水退化为完整握手连接）、无网卡调优权限
（txqueuelen 保持默认 1000）、无 sysctl 权限（源端口池/TIME_WAIT 均无法调整）。

---

## 2. 实测结果

| 指标 | 结果 |
|---|---|
| 总 PPS | **约 2,000,000 pps**（公网 SYN/UDP 约 1M + 内网 UDP 约 800K + 帧类数十万） |
| 总带宽 | 内网约 8-10Gbps + 公网约 2.5Gbps ≈ **10-12.5Gbps 压制** |
| 业务影响 | **完全崩溃**：页面无响应、连接建立全部超时 |
| 连接池 | **堵塞**：目标连接表/accept 队列被占满，合法连接无法进入 |
| 运维通道 | **SSH 无法运维**：新 SSH 连接无法建立，应急响应通道失守 |

---

## 3. 关键结论

### 3.1 非 root 内网攻击依然致命

攻击效果与权限相关性远低于直觉。按攻击类型拆解：

| 攻击类型 | 是否需要 root | 非 root 内网效果 |
|---|---|---|
| **带宽型（UDP 洪水）** | **否**（普通 UDP socket） | **10G 线速可达**——发送能力只受 CPU 限制，网卡调优缺失不影响线速（队列丢的只是超出线速的盈余包） |
| **应用层/连接池型（HTTP 洪水、HTTP/2 帧攻击）** | **否**（普通 TCP 连接） | 完全可用，连接复用率高，不受源端口池限制 |
| 连接风暴（TCP 连接洪泛） | 否（完整握手） | 受源端口池限制（默认约 2.8 万端口 / TIME_WAIT 60s ≈ 数百到数万连接/秒），但作为"连接骚扰 + 端口池消耗"依然有效 |
| root 专属（raw SYN 洪水、IP 伪造、反射放大） | **是** | 内网场景**非必需**：raw SYN 只是锦上添花；反射放大需要公网反射器池，内网本就无用 |

**本质**：带宽型与应用层攻击不依赖任何特权。**非 root 限制只削弱了"协议栈底层攻击"（raw SYN/spoof），削弱不了"流量与连接资源竞争"**——而后者才是压垮业务与连接池的主力。

### 3.2 内网低延迟放大攻击强度（与权限无关）

- 2ms RTT 下，完整握手连接每线程可达 500 连接/秒，是公网远程（80ms）的 **40 倍**
- TCP 窗口往返快，帧类攻击（HTTP/2）吞吐远高于公网
- 无公网丢包/重传/跨网拥堵损耗，带宽真实打满

### 3.3 目标保留公网入口时攻击更严重

内网被攻陷 + 目标同时暴露公网 = **双向压制、运维失守**：

1. **双入口叠加**：内网 10G + 公网 2.5G 同时压制，目标总承受约 12.5Gbps + 2M pps
2. **运维通道与业务共享协议栈**：SSH 与业务共用连接表/accept 队列，连接池被占满时**新 SSH 连接同样无法建立**——本次实测 SSH 完全无法运维
3. **应急响应失效**：管理员无法通过公网远程进入目标执行隔离/取证（SSH 被挤），而内网入口也被攻击流量占满——**"拔网线"成为唯一手段，且需要物理到场**
4. **告警外传被挤**：目标带宽打满后，安全设备日志/告警外传被挤压，检测响应链条被切断

---

## 4. 攻击原理拆解（各层贡献）

| 层次 | 手段 | 对目标的消耗 |
|---|---|---|
| 网络层 | UDP 10G 洪泛 | 网卡/中断/协议栈接收路径打满 |
| 传输层 | 连接洪泛（数万连接/秒） | conntrack 表、accept 队列、fd 耗尽 → **连接池堵塞** |
| 会话层 | HTTP/2 帧风暴（RST/CONTINUATION） | 目标 CPU（帧解析/内存分配），与带宽型叠加 |
| 应用层 | HTTP 洪水（raw 连续写） | 应用连接池、worker 线程、业务处理线程饿死 |

**崩溃路径**：连接池先满（业务与运维连接都无法建立）→ 协议栈 CPU 打满（2M pps 处理开销）→ 业务完全无响应 → 运维通道（SSH）随之失守。

---

## 5. 防御建议（按有效性排序）

1. **网络层隔离（根本）**：内网按业务分段（VLAN/ACL），被控主机所在网段与关键业务网段隔离；限制横向可达性——攻击的前提是"同网段可达"，切断可达性即切断攻击面
2. **运维冗余通道（应急）**：带外管理（BMC/iDRAC/独立管理网段）与业务网分离；SSH 限源 IP + 管理网独立带宽，确保应急通道不被业务流量挤占
3. **交换机侧限速与异常检测**：端口级 PPS/BPS 限速（防御性 QoS）、异常流量自动触发 ACL 隔离（交换机旁路检测）
4. **目标主机加固（延缓）**：调大 `somaxconn`/`tcp_max_syn_backlog`/`nf_conntrack_max`、连接数限制（`ulimit`/`systemd` LimitNOFILE）、应用层连接池上限与超时；注意这些只能**延缓崩溃**，挡不住 10G 级带宽压制
5. **监控与告警**：异常 PPS/连接数/丢包率检测，告警通道独立于业务网
6. **主机侧最小权限**：非 root 账户同样可以发起毁灭性带宽攻击——**"低权限"不等于"低风险"**，出网带宽限制（tc 限速）与每主机可发起流量的上限应在主机加固中一并考虑

---

## 6. 结论

- 非 root 内网节点可独立造成 **10G 带宽 + 连接池堵塞**级别的破坏，目标 8h16g 完全崩溃
- 配合公网节点协同，总压制 12.5Gbps / 2M pps，**业务崩溃 + 运维通道失守**
- 攻击可行性不依赖权限，只依赖**网络可达性**——内网任何主机被攻陷即具备横向打击能力
- 目标保留公网入口时，双入口压制使**应急响应（远程隔离）失效**，破坏半径进一步扩大
